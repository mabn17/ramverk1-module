language: php

php:
    - "7.2"
    - "7.3"
    - hhvm
    - nightly

sudo: false

git:
    submodules: false
    lfs_skip_smudge: true

matrix:
    allow_failures:
        - php: nightly
        - php: hhvm

before_script:
    - make install

script:
    - make check
    - make test

